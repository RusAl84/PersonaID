Run docker stack deploy -c stack.yml postgres
(or
docker-compose -f stack.yml up),
wait for it to initialize completely, and
visit http://swarm-ip:8080, http://localhost:8080, or http://host-ip:8080 (as appropriate).


docker service ls
Then, you can stop the service

docker service rm <service id discovered with previous command>


CREATE TABLE public.z1frame (
    id bigint NOT NULL,
    frame bytea,
    milliseconds numeric,
    timestr character(40)
);

ALTER USER personauser WITH PASSWORD 'pgpwd4persona';